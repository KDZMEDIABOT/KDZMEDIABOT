нада пер-юзер RAG collections OpenLink virtuozzo stores & RAG

- [ ] Создать пакет `orchestrator` в generic backend (`com.localmesalevel.aisystemtakeone.orchestrator`) — центральный оркестратор multi-LLM агентов. В него вбивается всё, что пришло в голову; ИИ кодит его прямо здесь (в этом пакете) и запускает тут же, в составе конкурентного backend процесса. Декомпозиция на подпакеты допускается по мере роста.
