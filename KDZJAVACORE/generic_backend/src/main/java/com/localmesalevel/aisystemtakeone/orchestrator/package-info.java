/**
 * Пакет {@code orchestrator} — центральный оркестратор multi-LLM агентов
 * (central orchestrator for multi-LLM agents).
 *
 * <p>Архитектурное назначение: это единая точка входа, в которую вбивается всё,
 * что приходит в голову. Сюда сбрасываются любые идеи, требования и команды, а
 * ИИ кодирует их прямо здесь, в этом пакете, и запускает тут же (в составе
 * конкурентного backend процесса). Пакет живёт внутри generic backend'а и
 * использует его инфраструктуру: Spring context, LLM сервисы (llm), RAG (rag),
 * WebSocket (websocket) и другие модули.
 *
 * <p>Пакет является "песочницей-оркестром": он может расширяться со временем
 * (модели, сервисы, контроллеры, конфигурация), декомпозиция на подпакеты
 * допускается по мере роста. Главное правило — никаких идей мимо оркестратора:
 * всё, что пришло в голову, попадает сюда и реализуется здесь.
 */
package com.localmesalevel.aisystemtakeone.orchestrator;